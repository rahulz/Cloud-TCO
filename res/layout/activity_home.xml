<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.cloudbia.cloudcompare.HomeActivity" >

    <Button
        android:id="@+id/buttonPublic"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/buttonPrivateVsPublic"
        android:layout_alignRight="@+id/buttonPrivateVsPublic"
        android:layout_centerVertical="true"
        android:onClick="showActivity"
        android:text="Comparison of Public Clouds" />

    <Button
        android:id="@+id/buttonPrivateVsPublic"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/buttonPublic"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="28dp"
        android:onClick="showActivity"
        android:text="Comparison of Public Clouds and Private Clouds" />

</RelativeLayout>
