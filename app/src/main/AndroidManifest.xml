<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cloudbia.cloudcompare"
    android:versionCode="1"
    android:versionName="1.0" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppBaseTheme" >
        <activity
            android:name=".activity.PublicCloudActivty"
            android:label="@string/app_name" >

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>

        <provider
            android:name=".contentProvider.LocalDbProvider"
            android:authorities="com.cloudbia.cloudcompare.contentProvider"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".activity.SplashActivty"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.HomeActivity"
            android:label="@string/title_activity_home" >
        </activity>
        <activity
            android:name=".activity.StartActivity"
            android:label="@string/title_activity_start" >
        </activity>
        <activity
            android:name=".activity.CompareCloudActivity"
            android:label="@string/title_activity_public_cloud" >
        </activity>
        <activity
            android:name=".activity.ComplareDisplayActivity"
            android:label="@string/title_activity_complare_display" >
        </activity>
    </application>

</manifest>
